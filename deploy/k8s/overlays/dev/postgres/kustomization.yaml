apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/postgres

namespace: talk-realm-dev

patches:
  - path: statefulset-patch.yaml
    target:
      kind: StatefulSet
      name: postgres
